# Tasks: Audit Log (MovementsPanel) + Invoice Editing

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~1650-1950 total (PR1 ~550-650: ports ~15, mock+db `invoice-repo.update` ~110, `payment-repo.ts` FOR UPDATE fix w/ doc comment ~80-120 (methodology-dependent), `updateInvoice` service ~40, tests incl. concurrency regression ~300-350 / PR2 ~625-700: migration ~30, ports ~20, mock+db `audit-log-repo` ~80, repositories/store/fixtures wiring ~40, `audit-log-service` ~30, `viewAuditLog` capability ~10, instrumentation call sites ~15, PATCH route ~50, tests ~350-400 / PR3 ~450-500: form edit-mode ~80, detail page action+panel wiring ~40, `movements-panel.tsx` ~80, tests ~250-300) |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | PR 1 (invoice-edit backend + mandatory empirical concurrency verification) → PR 2 (audit-log backend + instrumentation + PATCH route) → PR 3 (UI: edit form + MovementsPanel) |
| Delivery strategy | ask-on-risk (default; not overridden this session) |
| Chain strategy | feature-branch-chain (recommended, matching `inventario`/`nomina-payroll`'s 3-PR precedent; ask user to confirm before apply) |

Decision needed before apply: Yes
Chained PRs recommended: Yes
Chain strategy: feature-branch-chain
400-line budget risk: High

### Suggested Work Units

| Unit | Goal | Likely PR | Notes |
|------|------|-----------|-------|
| 1 | `InvoiceUpdate`/`InvoiceRepository.update` in `ports.ts`; `update` in both `lib/mock/invoice-repo.ts` and `lib/db/invoice-repo.ts`; the `lib/db/payment-repo.ts` concurrency fix (MANDATORY empirical verification against a real Postgres container BEFORE trusting any fix — see Phase 2); `updateInvoice` in `invoice-service.ts`; fully unit-tested (mock + db, both entities) | PR 1 | Base = feature/tracker branch. Self-contained backend slice; no UI, no audit table. Highest correctness risk in this change — do not shortcut the empirical verification step. |
| 2 | `migrations/1700000005000_add_audit_log.sql`; `AuditLogEntry`/`AuditLogCreate`/`AuditLogRepository` in `ports.ts`; dual-backend `audit-log-repo`; `repositories.ts`/`store.ts`/fixtures wiring; `audit-log-service.ts` (`recordAuditLog`/`listAuditLog`); `viewAuditLog` capability; instrumentation call sites in `createInvoice`/`updateInvoice`/`createPayment`; `PATCH /api/invoices/[id]` | PR 2 | Base = PR 1 branch. Depends on PR 1's `updateInvoice` existing to instrument. Session-gated only (no capability gate on the route itself — editing is not admin-only, only VIEWING the audit trail is). |
| 3 | `invoice-form-content.tsx` edit-mode support; "Editar factura" action (zero-payments only) + `<MovementsPanel>` (call-site `can()` gate) on `app/(dashboard)/invoices/[id]/page.tsx`; new `components/domain/audit-log/movements-panel.tsx` | PR 3 | Base = PR 2 branch. Depends on PR 1's PATCH route/service and PR 2's `listAuditLog`/`viewAuditLog`. This is the app's FIRST widget-level (not whole-page) role gate — treat the worker-sees-page-but-not-panel distinction as an explicit acceptance criterion, not an assumption. |

## Phase 1: Invoice-Edit Foundation (Ports & Types) (PR1)

- [x] 1.1 `lib/services/ports.ts`: add `InvoiceUpdate` type (`customerId`, `issueDate`, `dueDate?`, `items: InvoiceItemInput[]`, `notes?` — `number` deliberately excluded, immutable) and add `update(businessId, id, data: InvoicePersist): Promise<InvoiceDetail | null>` to `InvoiceRepository`, documented as re-verifying zero-payments atomically (defense in depth vs. the service check).

## Phase 2: Invoice-Edit Repositories + MANDATORY Empirical Concurrency Verification (PR1)

- [x] 2.1 Implement `update` in `lib/mock/invoice-repo.ts`: run inside `withLock(invoiceId)` (SAME key `lib/mock/payment-repo.ts` uses for `createForInvoice`); inside the lock, `paymentsForInvoice(store, id).length > 0` → throw `ApiError("CONFLICT", ...)` with zero mutation; else replace `invoiceItems` for that invoice and update the header (recomputed `subtotal`/`total`/`status`, `number` untouched).
- [x] 2.2 Implement `update` in `lib/db/invoice-repo.ts`: pre-`SELECT` for existence/business-scope (missing → `NOT_FOUND`); then a guarded header `UPDATE ... WHERE id=$id AND business_id=$businessId AND NOT EXISTS (SELECT 1 FROM payments WHERE invoice_id=invoices.id) RETURNING id`; zero rows AND pre-SELECT found it → `ApiError("CONFLICT", ...)`; on success, `DELETE FROM invoice_items WHERE invoice_id=$id` then re-`INSERT` the new items, only after the guarded `UPDATE` returns a row.
- [x] 2.3 **MANDATORY, do this BEFORE writing/trusting any `lib/db/payment-repo.ts` fix**: stand up a real Postgres 16 container (Docker), load this change's schema (existing migrations + the invoice/payment tables — no audit_log needed for this test), and reproduce the race: fire two genuinely concurrent requests against the same invoice — one calling `invoice-repo.ts#update`'s guarded `UPDATE`, one calling `payment-repo.ts#createForInvoice`'s existing overpay-guard CTE — against an invoice seeded with zero payments and a positive balance. Confirm whether both can commit (edit succeeds AND a payment is recorded against post-edit-invalid state, or vice versa) with the CURRENT (unmodified) `payment-repo.ts`. Document the observed outcome (mirrors the `inventario` methodology: fresh container each run, 3x repetition). **DONE — confirmed BROKEN 6/6 (3/3 payment-first, 3/3 edit-first), against a real Postgres 16 Docker container.**
- [x] 2.4 Add `FOR UPDATE` to `lib/db/payment-repo.ts`'s existing overpay-guard CTE's `invoices i` read (the design's proposed fix) and re-run the SAME concurrent-race harness from 2.3, 3x, against a fresh container each time. Per this session's established lesson (a `FOR UPDATE`-in-single-CTE fix for `inventario`'s guard LOOKED correct by inspection but was proven broken — EvalPlanQual only re-checks the locked row's OWN columns on lock-wait resume, not a correlated subquery reading a different table in the same statement): explicitly verify whether `payment-repo.ts`'s CTE has the SAME shape of hazard (its CTE locks `invoices i` while SUM-aggregating the sibling `payments` table in that same statement — same class of risk) or is structurally different enough that a single-statement `FOR UPDATE` is actually sufficient here. Do not assume either way — check empirically. **DONE — confirmed STILL BROKEN 3/3 (payment-first ordering): the payment's FOR UPDATE lock never modifies the `invoices` row itself, so the blocked edit's own `NOT EXISTS` subquery has nothing to trigger EvalPlanQual and resumes with a stale (pre-payment) result.**
- [x] 2.5 If 2.4's harness shows the single-statement `FOR UPDATE` does NOT close the race (mirroring `inventario`'s outcome): replace it with a genuine two-statement `sql.transaction([...])` in `lib/db/payment-repo.ts` — statement 1 `SELECT id FROM invoices WHERE id=$id AND business_id=$businessId FOR UPDATE` (acquire+hold the lock), statement 2 the existing balance-CTE `INSERT` (fresh snapshot only after the lock resolves), mirroring `lib/db/inventory-repo.ts`'s verified two-statement pattern exactly (including its file-level doc-comment style explaining why). Re-verify 3/3 against a real Postgres container reproducing both the still-broken single-statement version and the fixed version's correctness. **DONE — the two-statement fix was applied symmetrically to BOTH `payment-repo.ts#createForInvoice` AND `invoice-repo.ts#update` (both writers must lock the same row); re-verified CORRECT 10/10 (5 payment-first + 5 edit-first, including a downward-edit regression case) against a real Postgres 16 container.**
- [x] 2.6 (N/A — 2.5's branch was taken; the single-statement fix was proven insufficient, not sufficient.) Either way (2.5 or 2.6), update `openspec/changes/audit-log/design.md`'s "Open Questions" section to record the resolved decision and evidence before this task is considered done. **DONE — see design.md's "Open Questions" section.**
- [x] 2.7 Update `lib/services/invoice-service.ts`'s doc comment / `design.md` cross-reference as needed to point at whichever fix (2.5 or 2.6) actually shipped, so future readers don't re-litigate the resolved question. **DONE — `updateInvoice`'s doc comment cross-references design.md's "Edit-Lock Race Mechanism"; the full methodology and evidence live in `lib/db/payment-repo.ts`'s and `lib/db/invoice-repo.ts`'s file-level doc comments.**

## Phase 3: Invoice-Edit Service (PR1)

- [x] 3.1 `lib/services/invoice-service.ts`: add `updateInvoice(session, id, data)` — resolve via `getInvoice(session, id)` (reuses `getById` → `withFinance` → `computeStatus`, NO independent re-summing of payments), reject with `ApiError("CONFLICT", ...)` if `invoice.paidAmount !== 0`; re-validate item invariants identically to `createInvoice` (quantity > 0, unitPrice >= 0) before calling the repo; recompute `subtotal`/`total`/`status` server-side; `customerId` re-validated via `repositories.customers.getById` (cross-business/missing → `NOT_FOUND`); `number` never accepted from `data`; call `repositories.invoices.update(session.businessId, id, persist)`, mapping a `null` result to `NOT_FOUND`.

## Phase 4: PR1 Tests (Invoice-Edit Backend)

- [x] 4.1 `lib/mock/invoice-repo.test.ts` + `lib/db/invoice-repo.test.ts`: `update` on a zero-payment invoice replaces items and recomputes `subtotal`/`total`/`status`, `number` unchanged; `update` on an invoice with any payment rejects with `CONFLICT` and zero mutation (snapshot store/mocked `sql` before/after, mirroring `payment-service.test.ts`'s partial-state-impossibility style); cross-business `id` → `NOT_FOUND`.
- [x] 4.2 `lib/services/invoice-service.test.ts`: `updateInvoice` rejects before reaching the repository when `paidAmount>0` (mock the repo's `update` to assert it is never called); rejects unknown `customerId` (`NOT_FOUND`); rejects invalid items (`VALIDATION_ERROR`) before any repo call; client-forged `status`/`total`/`subtotal`/`number`/`businessId` fields are discarded, matching `createInvoice`'s established convention.
- [x] 4.3 Regression test for whichever `payment-repo.ts` fix shipped (2.5 or 2.6): a concurrency-shaped test proving the edit/first-payment interleave cannot leave both a committed payment and a committed edit against the pre-edit total (mirrors `inventory-repo.test.ts`'s DB test asserting the guarded statement's structure, plus — if Docker is available in CI — an integration-level test that actually exercises two concurrent calls). **Structural regression tests in `lib/db/invoice-repo.test.ts`/`lib/db/payment-repo.test.ts` (mocked `sql`/`sql.transaction`, asserting the two-statement lock-then-check/insert shape AND — post-review — the interpolated VALUES of every statement, not just its text). FOLLOW-UP FIX PASS: the concurrent-integration verification is now a COMMITTED, re-runnable test at `lib/db/invoice-payment-concurrency.integration.test.ts` (real Postgres 16 via Docker, two `pg` connections, deterministic hold-open overlap, both orderings x5 each), gated behind `describe.skipIf(!process.env.RUN_DB_INTEGRATION_TESTS)` so it is excluded from the default `npm test`/CI run (no standing test-DB infra) but easy to run manually — see the file header and design.md. Re-run locally: PASS 2/2, deterministic.**

## Phase 5: Audit Log Schema, Ports & Dual-Backend Repos (PR2)

- [x] 5.1 Create `migrations/1700000005000_add_audit_log.sql`. Up: `audit_log` (`id UUID PK`, `business_id UUID NOT NULL REFERENCES businesses(id)`, `entity_type TEXT NOT NULL`, `entity_id UUID NOT NULL`, `action TEXT NOT NULL`, `actor_user_id UUID NOT NULL`, `detail TEXT`, `created_at TIMESTAMPTZ NOT NULL DEFAULT now()` — NO `CHECK` constraints on `entity_type`/`action`, extensible by design) + index `idx_audit_log_entity ON audit_log(business_id, entity_type, entity_id)`. Down: `DROP TABLE IF EXISTS audit_log CASCADE`. **DONE.**
- [x] 5.2 `lib/services/ports.ts`: add `AuditLogEntry` (`id`, `businessId`, `entityType`, `entityId`, `action`, `actorUserId`, `detail: string | null`, `createdAt`), `AuditLogCreate` (`entityType`, `entityId`, `action`, `actorUserId`, `detail?`), `AuditLogRepository` (`list(businessId, entityType, entityId): Promise<AuditLogEntry[]>` ordered `created_at DESC`; `create(businessId, data): Promise<AuditLogEntry>` — list/create only, no update/delete surface). **DONE.**
- [x] 5.3 Create `lib/mock/audit-log-repo.ts`: append-only (Expense-style — no `withLock`, no balance invariant), `list` business-scoped + filtered by `entityType`/`entityId`, sorted `createdAt` DESC; `create` derives `businessId` always from the arg, never from `data`. **DONE.**
- [x] 5.4 Create `lib/db/audit-log-repo.ts`: Postgres mirror via parameterized SQL (mirrors `lib/db/expense-repo.ts`'s shape). **DONE.**
- [x] 5.5 `lib/services/repositories.ts`: wire `auditLog: isDbConfigured ? dbAuditLogRepo : mockAuditLogRepo`. **DONE.**
- [x] 5.6 `lib/mock/store.ts`: add `auditLogs: Map<string, AuditLogEntry>` to `MockStore`/`SerializedStore`; wire into `serializeStore`/`clearStore`/`createEmptyStore`; `hydrateStore` uses `?? []` defensive fallback (cookie backward-compat, matching every prior phase's precedent). **DONE** — regression tests added to `lib/mock/store.test.ts`.
- [x] 5.7 `lib/mock/fixtures/data.ts` + `lib/mock/fixtures/index.ts`: (optional) seed a few demo `AuditLogFixture` rows for existing seeded invoices, scoped to `BUSINESS_ID`; confirm `seedMinimal` does NOT seed audit rows (cookie-size constraint, matching prior entities). **SKIPPED per this task's own "optional"/"use your judgment" note** — this is a trailing/incidental entity, not a primary demo feature; the mandatory `?? []` hydration guard (5.6) is done regardless. `seedMinimal`/`seedFixtures` seed zero audit rows, which trivially satisfies "confirm `seedMinimal` does NOT seed audit rows."

## Phase 6: Audit Service, Permissions, Instrumentation & PATCH Route (PR2)

- [x] 6.1 Create `lib/services/audit-log-service.ts`: `recordAuditLog(session, entityType, entityId, action, detail?)` — wraps `repositories.auditLog.create(session.businessId, { entityType, entityId, action, actorUserId: session.userId, detail })` in try/catch; on failure, `console.error` and return (NEVER rethrow — the parent mutation already succeeded). `listAuditLog(session, entityType, entityId)` — thin pass-through to `repositories.auditLog.list`. **DONE.**
- [x] 6.2 `lib/services/permissions.ts`: add `"viewAuditLog"` to the `Capability` union, `viewAuditLog: ["admin"]` to `CAPABILITY_ROLES`, and `canViewAuditLog(role)` helper — mirrors `canViewPayroll`'s exact pattern. **DONE.**
- [x] 6.3 Instrument `lib/services/invoice-service.ts`: after `createInvoice` succeeds, call `recordAuditLog(session, "invoice", invoice.id, "invoice_created")`; after `updateInvoice` succeeds, call `recordAuditLog(session, "invoice", invoice.id, "invoice_updated")` — both AFTER the repository call resolves, not wrapped in its transaction. **DONE** (detail carries the invoice's `number`).
- [x] 6.4 Instrument `lib/services/payment-service.ts`: after `createPayment` succeeds, call `recordAuditLog(session, "invoice", invoiceId, "payment_recorded")`. **DONE** (detail carries the recorded amount).
- [x] 6.5 Create `PATCH` handler in `app/api/invoices/[id]/route.ts`: `requireSession()` + `checkOrigin(request)` (session-gated only, NO capability check — editing is not admin-restricted), parse body, validate with a new `.strict()` `invoiceUpdateSchema` (`lib/schemas/invoice.ts`, mirrors `invoiceCreateSchema` minus `number`/other forged fields), call `updateInvoice`, return `{ data: invoice }` 200; `CONFLICT`/`NOT_FOUND`/`VALIDATION_ERROR` propagate via `withApiHandler`'s existing `ApiError` mapping. **DONE.**

## Phase 7: PR2 Tests (Audit Log Backend + Instrumentation)

- [x] 7.1 `lib/mock/audit-log-repo.test.ts` + `lib/db/audit-log-repo.test.ts`: business-scoped isolation; `list` filters by `entityType`/`entityId` and orders `createdAt` DESC; no update/delete method exists on the interface. **DONE.**
- [x] 7.2 `lib/services/audit-log-service.test.ts`: `recordAuditLog` swallows a rejecting `repositories.auditLog.create` (mocked to throw) without rethrowing; `listAuditLog` passes through correctly scoped by business. **DONE** (also covers a synchronously-throwing mock).
- [x] 7.3 `lib/services/invoice-service.test.ts` additions: `createInvoice` triggers `recordAuditLog` with `action="invoice_created"`/`entityType="invoice"`; `updateInvoice` triggers it with `action="invoice_updated"`; mocking the audit repo's `create` to reject proves `createInvoice`/`updateInvoice` still succeed (best-effort, parent mutation unaffected). **DONE** (also proves no audit row is attempted when the edit-lock CONFLICT rejects first).
- [x] 7.4 `lib/services/payment-service.test.ts` addition: `createPayment` triggers `recordAuditLog` with `action="payment_recorded"`, `entityType="invoice"`, `entityId=<invoiceId>`; audit-repo rejection does not fail the payment. **DONE** — exercised against the REAL mock `auditLogRepo`/store (not a mocked `repositories` module), proving the end-to-end wiring; also proves no row is written on a rejected overpay.
- [x] 7.5 `lib/services/permissions.test.ts` additions: `canViewAuditLog("admin")` → `true`; `canViewAuditLog("worker")` → `false`; `can(role, "viewAuditLog")` mapping equivalence. **DONE.**
- [x] 7.6 Route tests (`app/api/invoices/[id]/invoices-id-route.test.ts` or existing route test file): `PATCH` succeeds for any authenticated session (no role restriction) on a zero-payment invoice; 401 anon; `CONFLICT` (409) on a paid invoice with zero mutation; cross-business → `NOT_FOUND`; `checkOrigin` enforcement. **DONE** — added to the existing `app/api/invoices/invoices-routes.test.ts` (mirrors the PATCH pattern already used in `app/api/employees/employees-routes.test.ts`), including the strict-schema forged-field rejection and the `invoice_updated` audit-row assertion.

## Phase 8: Invoice-Edit UI + MovementsPanel (PR3)

- [ ] 8.1 `components/domain/invoices/invoice-form-content.tsx`: add optional `invoice?: InvoiceDetail`-shaped prop (pre-fills `customerId`/`issueDate`/`dueDate`/`items`/`notes` as default form values when present); switch `onSubmit` to `PATCH /api/invoices/{id}` when editing (`fetch` method + URL branch on `invoice` presence), keep `POST /api/invoices` for create; button label "Guardar cambios" vs. "Crear factura"; `router.push`/`router.refresh()` to the invoice detail page on success either way.
- [ ] 8.2 `app/(dashboard)/invoices/[id]/page.tsx`: add an "Editar factura" action (rendered only when `invoice.paidAmount === 0`), linking to an edit route/dialog that reuses `InvoiceFormContent` with the `invoice` prop; add `<MovementsPanel invoiceId={invoice.id} />` gated by a PLAIN `can(session.role, "viewAuditLog")` boolean check at THIS call site — do NOT use `requireCapabilityOrNotFound` (that would 404 the whole page for workers); the rest of the page stays reachable/functional for all sessions.
- [ ] 8.3 Create `components/domain/audit-log/movements-panel.tsx`: read-only Server Component mirroring the Card+Table+empty-state shape already used for the invoice detail page's Pagos section — calls `listAuditLog(session, "invoice", invoiceId)` inline (no extra API route), renders `action`/`detail`/`createdAt` rows, "Sin movimientos registrados." empty state.

## Phase 9: PR3 Tests

- [ ] 9.1 `invoice-form-content.test.tsx` additions: edit mode pre-fills all fields from the `invoice` prop; valid edit submit `PATCH`es the correct payload and cents-converted `unitPrice`; create mode unaffected (regression); the "Editar factura" action/edit form is only reachable/enabled when `paidAmount === 0` — add an explicit test proving the action is hidden or disabled once ANY payment exists on the invoice.
- [ ] 9.2 `app/(dashboard)/invoices/[id]/page.test.tsx` additions: admin session sees `<MovementsPanel>` on the invoice detail page; worker session does NOT see the panel BUT the rest of the invoice detail page still renders (200, full content, no 404/redirect) — these must be two distinct assertions in the same or paired tests, not conflated.
- [ ] 9.3 `components/domain/audit-log/movements-panel.test.tsx`: renders rows for a given invoice's audit history in `createdAt` DESC order; empty-state renders when no rows exist; only rows matching `entityType="invoice"`/`entityId`/session `businessId` are shown (scoping regression).

## Phase 10: Verification Gate

- [ ] 10.1 `npm run typecheck`
- [ ] 10.2 `npm run lint`
- [ ] 10.3 `npm run test` (including a shuffled-seed run — meaningful now per the `resetStore` fix from `inventario`)
- [ ] 10.4 `npm run build`
